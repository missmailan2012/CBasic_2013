#include<stdio.h>
#include<stdlib.h>

int main()
{
  void *ptr;
  ptr=malloc(sizeof(char)*10);
  if(ptr == NULL);
  printf("Con tro ptr tro den vi tri: 0x%p\nCon tro ptr+1 tro den vi tri: 0x%p\n",ptr,ptr+1);
  return 0;
}
